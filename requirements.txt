# Pinned for reproducibility across Colab and the UZH cluster.
# Adjust torch to match the cluster's CUDA version when you get access.
torch==2.3.1
transformers==4.44.2
peft==0.12.0
datasets==2.21.0
sacrebleu==2.4.3
ijson==3.3.0
pyyaml==6.0.2
numpy==1.26.4
python-dotenv==1.0.1
